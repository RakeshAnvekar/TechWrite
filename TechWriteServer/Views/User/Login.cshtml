@model TechWriteServer.Models.User.User

@{
    ViewData["Title"] = "User Login";
    var userLoginName = (string?)ViewData["loginUserName"];
}
<form asp-action="Login" method="post">
    
    <div class="form-group">
        <label for="Email">Email <span style="color:red">*</span></label>
        <input type="email" asp-for="UserEmailId" id="userEmailId" class="form-control" />
        <span asp-validation-for="UserEmailId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="Password">Password <span style="color:red">*</span></label>
        <input type="password" asp-for="Password" id="userPassword" class="form-control" />
        <span asp-validation-for="Password" class="text-danger"></span>
    </div>    
    <button type="submit" class="btn btn-primary" id="btnLogin">Login</button>
</form>

